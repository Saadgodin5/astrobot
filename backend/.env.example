# Server
PORT=3000
NODE_ENV=development

# JWT authentication
JWT_SECRET=your_secret_key_here

# PostgreSQL (local dev: use localhost; Docker: use postgre_container)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydatabase
DB_USER=tidiane
DB_PASSWORD=tidkon

# Optional: n8n webhook for chat
# N8N_WEBHOOK=http://localhost:5678/webhook/astrobot

# Optional: HuggingFace API for image generation
# HF_API_KEY=your_hf_key
